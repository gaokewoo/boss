#   群组成员类型	GROUP_MEMBER_TYPE
	MEMBER_TYPE_ID			NUMBER(9),   
	MEMBER_TYPE_NAME        VARCHAR(50) 	NOT NULL,
	MEMBER_TYPE_DESC        VARCHAR(250)	NOT NULL,
	constraint PK_GROUP_MEMBER_TYPE primary key (MEMBER_TYPE_ID)
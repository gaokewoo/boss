#	群组成员角色	GROUP_INSTANCE_ROLE	
	MEMBER_ROLE_ID 			NUMBER(9),   
	MEMBER_ROLE_NAME        VARCHAR(50)		NOT NULL,
	MEMBER_ROLE_DESC		VARCHAR(250)	NOT NULL,
	constraint PK_GROUP_INSTANCE_ROLE primary key (MEMBER_ROLE_ID)